const initialState = {
    tasks: [
        'house clean',
        'loundry service',
        'work out'
    ]
};

const todo = (state = initialState, action) => {
    return state;
};

export default todo;
